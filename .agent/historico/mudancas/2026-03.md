# Historico de Mudancas - 2026-03

## 2026-03-05

- Iniciada `TASK-001` para integrar sync de pricing/materials entre Terminal e Portal.
- Estrutura obrigatoria de contexto `.agent/` criada e normalizada para continuidade entre chats.
- Planejamento ativo registrado para orientar implementacao incremental e validacoes.
- Documento tecnico versionado criado: `docs/INTEGRACAO_TERMINAL_PORTAL_ALUNO_SYNC.md` (v1.0.0).
- Implementados `PortalSyncService` e `PortalPricingConfigService`.
- Implementado modulo admin de pricing com rotas, controller e views (`/secure/adm/aluno/pricing`).
- Validacao de sintaxe executada via `php -l` nos arquivos alterados (sem erros de sintaxe).
- Documentacao atualizada para `v1.1.0` com cobertura de materiais.
- Implementados `PortalMaterialsConfigService` e `AdminAlunoMaterialsController`.
- Implementadas views de materiais (`aluno_materials_index` e `aluno_materials_form`) e rotas `/secure/adm/aluno/materials*`.
- Upload inicial de materiais habilitado em `public/uploads/aluno/materials` com extensoes controladas e limite de 50MB.
- Implementada auditoria local de tentativas de sync em `.config/portal_sync_audit.json` via `PortalSyncAuditService`.
- Implementado `BunnyStorageService` para upload de materiais em CDN (com fallback local).
- Variaveis de ambiente de Bunny Storage adicionadas em `.env.example`.
- Documentacao de integracao atualizada para `v1.3.0`.
- Implementado painel admin de auditoria/retry em `/secure/adm/aluno/sync-audit`.
- Check tecnico de conectividade com Portal executado na CLI e bloqueado por erro SSL de cadeia CA local.
- `.env` atualizado com `TERMINAL_SYNC_SECRET` (64 bits), variaveis de Portal Sync e ajustes Bunny Stream (`SIGNING_KEY`, `TOKEN_TTL=600`).
- Decisao arquitetural registrada: pivot de media delivery para Hostinger + PHP/.htaccess (despriorizando Bunny como storage principal).
- Documento de arquitetura criado: `docs/ARQUITETURA_MEDIA_DELIVERY_HOSTINGER.md`.
- Plano de finalizacao de entrega da `TASK-001` detalhado em `.agent/operacional/planos/ativos/TASK-001-plano-integracao-portal-sync.md`, incluindo validacao em VPS quando ambiente local estiver bloqueado por SSL.
- Implementado `MediaOriginService` para upload em origin dedicado Hostinger (auth bearer + parse JSON + timeout configuravel).
- Fluxo de upload de materiais no admin alterado para prioridade Hostinger, com fallback sequencial para Bunny e local.
- Materiais agora persistem `storage_path`/`storage_driver` para suportar operacao de media e links assinados.
- Sync de materiais para cursos gera URL assinada para itens restritos quando `MEDIA_ORIGIN_DOWNLOAD_BASE_URL` e `MEDIA_ORIGIN_SIGNING_KEY` estao configurados.
- Preview de material restrito no admin passou a usar URL assinada quando disponivel.
- `.env.example` e docs de integracao/arquitetura atualizados para variaveis `MEDIA_ORIGIN_*` e estrategia de assinatura.
- Validacao tecnica local executada (`php -l`) nos arquivos alterados, sem erros de sintaxe.
- Diagnostico de configuracao executado via CLI: `MEDIA_ORIGIN_BASE_URL`, `MEDIA_ORIGIN_UPLOAD_TOKEN`, `MEDIA_ORIGIN_DOWNLOAD_BASE_URL` e `MEDIA_ORIGIN_SIGNING_KEY` ainda estao vazias no ambiente atual.
